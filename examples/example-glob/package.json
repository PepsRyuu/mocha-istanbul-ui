{
    "scripts": {
        "test": "mocha-istanbul-ui \"test/**/*.js\" --instrument --console --once",
        "test:ui": "mocha-istanbul-ui \"test/**/*.js\" --instrument --watch"
    },
    "devDependencies": {
        "chai": "^4.2.0",
        "mocha-istanbul-ui": "file:../.."
    }
}

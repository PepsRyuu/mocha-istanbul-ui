import { Component } from 'preact';
import './MyComponent.scss';

export default class MyComponent extends Component {
    render () {
        return (
            <div class="MyComponent">Hello World</div>
        );
    }
}